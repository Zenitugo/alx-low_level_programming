#include "main.h"

/**
 * main - check the code.
 * @c: is uppercase
 * Return: (1) if successful other wise return (0)
 */

int _isupper(int c);
{
	char c;

	if (c = 'A' && c <= 'Z')
	{
		_putchar("%c: %d\n", c _isupper(c));
	}
	else (c = 'a' && c <= 'z')
	{
		continue;
		_putchar(c);
		c++
	}
	_putchar("\n");
	return (1);
}
	
