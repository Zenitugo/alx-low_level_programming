#include <stdio.h>

/**
 * main - entry point
 * takes no argument
 *
 * Description: print alphabet in lowercase
 * Return: zero if successful
 */
int main(void)
{
	int ch;

	ch = 'a' ;
	while
	(ch <= 'z');

	{
		putchar(ch);
		ch++;
	}

	putchar ('/n');
	return (0);
}
