#include <stdio.h>

/**
 * main - entry point
 * takes no argument
 *
 *Descripton: lower case then uppercase
 *Return: zero if successful
 */
int main(Void)
{
	int ch;

	if ch = 97;
		while (ch <= 122);

		{
			putchar(ch;)
			ch++;
		}

	ch = 65
		while (ch <=90)
		{
			putchar(ch);
			ch++;
		}
	putchar(10);
	return (0)
}
