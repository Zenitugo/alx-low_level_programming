#include "main.h"

/**
 * add - entry point
 * @m: first argument
 * @n: second argument
 * Description:  function that adds two integers and returns the result
 * Return: value of the sum
 */

int add(int m,  int n)
{
	int sum;

	sum = (m + n)

	return (sum);
}
