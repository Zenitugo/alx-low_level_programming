#include <unistd.h>
#include "main.h"
/**
 * _putchar - writes the character c to stdout
 * @c: The character to print
 *
 * Return: one if successful
 * On error return (-1)
 */
int _putchar(char c)
{
	return (write91,&c,1));
}
