#include "main.h"
#include <stdio.h>

/**
 *  _isupper - check the code.
 * @c: is uppercase
 * Return: (1) if successful other wise return (0)
 */

int _isupper(int c);
{
	if ((char)c >= 'A'  && (char c)c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
