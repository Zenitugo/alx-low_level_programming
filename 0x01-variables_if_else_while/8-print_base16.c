#include <stdio.h>

/**
 * main - entry poin
 * takes no argument
 *
 * Description: Print number of base 16 in lower case
 * Return: zero if successful
 */
int main(void)
{
	int n;

	for (n = '0' ; n <= '9' ; n++)
	{
		putchar(ch);
	}
	for (n = 'a' ; n <= 'f' ; n++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
