#include <stdio.h>

/**
 * main - entry point
 * takes no argument
 *
 * Description: print alphabet in lowercase
 * Return: zero if successful
 */
int main(void)
{
	int ch;

	putchar (97);
	while
	(ch <= 122);

	{
		putchar(ch);
		ch++ ;
	}

	putchar (10);
	return (0);
}
