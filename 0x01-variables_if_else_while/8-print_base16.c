#include <stdio.h>

/**
 * main - entry poin
 * takes no argument
 *
 * Description: Print number of base 16 in lower case
 * Return: zero if successful
 */
int main(void)
{
	int n;

	for (n = 48 ; n <= 57 ; n++)
	{
		putchar(ch);
	}
	for (n = 97 ; n <= 102 ; n++)
	{
		putchar(ch);
	}
	putchar(10);
	return (0);
}
