#include "main.h"

/**
 *  _isupper - check the code.
 * @c: is uppercase
 * Return: (1) if successful other wise return (0)
 */

int _isupper(int c);
{
	char c;

	if (c = 'A' && c <= 'Z')
	{
		return (1)
	}
	else
	{
		return (0)
	}
}
