#include "main.h"
#include <stdio.h>

/**
 *  _isupper - check the code.
 * @c: is uppercase
 * Return: (1) if successful other wise return (0)
 */

int _isupper(int c);
{
	if (c >= 65 && c <= 90)
	return (1);
	return (0);
}
